Build the initial public gctx CLI in /Users/kellen/development/github/kellen-miller/gctx/.worktrees/initial-gctx on feat/initial-gctx, following decision.md and execplan.md. Implement in Go 1.26 with urfave/cli/v3 and a root main package. gctx must dynamically discover native gcloud configurations; fuzzy-select with external fzf; support gctx NAME, gctx --current/-c, and gctx -; require explicit human account, project, and billing/quota_project; reject GOOGLE_APPLICATION_CREDENTIALS and CLOUDSDK_ACTIVE_CONFIG_NAME on every path; synchronize the single global ADC with auth login --brief --no-activate --update-adc and set-quota-project before activating the named configuration; preserve exact prior ADC via an opaque same-directory 0600 backup on failure; and persist kubectx-style previous context securely and atomically. Do not hardcode or publish any personal account/project values, and keep .agent local-only. Build test-first with fake executables and cover parsing, validation, cancellation, order, ADC restoration/removal, state behavior, permissions, and failures. Add neutral README setup/recovery documentation using native gcloud commands, MIT metadata, Taskfile, pinned developer tooling outside the root module graph, golangci-lint v2 config, and pinned GitHub-hosted PR CI for fmt/lint/vuln, race tests, and builds on Ubuntu/macOS. Run full local checks, perform a live smoke test using the user's four local contexts without committing their values, restore the preferred context, run fresh and adversarial implementation reviews, fix verified findings, commit with Conventional Commits, push using git push origin HEAD:refs/heads/feat/initial-gctx, open a PR, watch all checks to green, and leave the PR open without merging or publishing a release. Completion evidence must include passing gofmt, vet, golangci-lint, govulncheck, actionlint, go test -race ./..., go build, exact public CLI behavior, real gcloud/ADC alignment and gctx - restoration, a clean tracked-file privacy scan, the PR URL, and live green GitHub checks.
